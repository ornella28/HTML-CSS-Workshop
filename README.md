**Report**
##
1. I started my project HTML-CSS workshop by creating a new folder on my local machine. Then, inside that file created locally, I opened it with visual studio code using the terminal by tapping: code
   
2. I then created three files with Visual studio code, one which will contain HTML code, another one which will contain CSS styles and the last one that will contain images and icons.

3. Before coding, I draw on the paper howI want my page to look like, what I wanted to include on the page and the disposition.

4. I then proceeded with the coding part. I created a HTML code, with header, body and footer. I wanted to have on my pages 3 tables for menus and opening hours, and a form for making commands with a button.
 I used Css to achieve that.
5. In the CSS files, I organized the tables. 2 tables and a form will be on the same line, the form will be placed between the two tables. The last table will be placed at the bottom in the middle
I inserted an image in the background and another image in the header as a background too.
6. I created an ignore file locally, then commit and push remotely on github
7. I created a README.md file remotely to write my report and then cloned it to sync with my local project
8. ...

